import reducers, { AppconfState, INITIAL_STATE } from './reducers';
import * as appconfActions from './actions';
import appconfTypes from './types';

export { appconfActions, appconfTypes, AppconfState, INITIAL_STATE };

export default reducers;
